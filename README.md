first_airflow_dag-


In this project i created a simple dag in airflow , an weather dag that extract, 
transforme et load data weather each day from a open-meteo web site and will create a file csv
for each day first i created the ETL pipline without airflow to see what the prosses wich airflow will orchestrate
